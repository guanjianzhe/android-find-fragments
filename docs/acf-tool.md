# acf 工具简介

`acf`（Android Current Fragments）是一个命令行工具，用于通过 ADB 快速定位当前前台 `Activity` 及其包含的 `Fragment`，并映射到本地源码。命名参考了 `adb` 的三字母缩写，简洁易记。

## 核心功能

- 获取当前前台 Activity
- 列出 Activity 中的 Fragments
- 搜索本地源码文件，可选打开结果

更多用法请参考项目 [README](../README.md)。

